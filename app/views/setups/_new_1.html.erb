<%= form_for(@user, as: :user, url: create_setup_path(step: step || '1'), method: :post) do |f| %>
  <div id="setup-container">
    <p class="setup-overview-text">
      It is easy to get started with thredded.
      By following these simple steps you will be on your way to creating
      your own community.
    </p>

    <dl>
      <dt><%= f.label :name, 'Username' %></dt>
      <dd><%= f.text_field :name %></dd>

      <dt><%= f.label :email %></dt>
      <dd><%= f.text_field :email %></dd>

      <dt><%= f.label :password %></dt>
      <dd><%= f.password_field :password %></dd>

      <dt><%= f.label :password_confirmation %></dt>
      <dd><%= f.password_field :password_confirmation %></dd>
    </dl>

    <%= f.submit 'Continue', :class => 'btn setup-continue-btn' %>
  </div>
<% end %>
