<% if user_signed_in? %>
  <% if current_user.superadmin? %>
    <li> <%= link_to('Admin', edit_admin_site_path(site.subdomain)) %> </li>
  <% elsif messageboard && current_user.admins?(messageboard) %>
    <li> <%= link_to('Admin', edit_admin_site_messageboard_path(site.subdomain, messageboard.name)) %> </li>
  <% end %>
<% end %>
